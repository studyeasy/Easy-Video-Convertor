# Build / dev artifacts (also ignored inside Source Code/)
.venv/
build/
dist/
__pycache__/
*.pyc

# Bundled FFmpeg binaries (~170 MB) are fetched by scripts/get-ffmpeg.ps1
vendor/ffmpeg/

# Installers are hosted on tools.dubnext.com (R2), not in the repo
*.exe
